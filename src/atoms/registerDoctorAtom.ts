import { atom } from "recoil";

//!---------------------------------------------------------------------------------!//

const registerDoctorScreenAtom = atom({
    key: 'registerDoctorScreenAtom',
    default: 'firstPart'
});

//!---------------------------------------------------------------------------------!//

export default registerDoctorScreenAtom;